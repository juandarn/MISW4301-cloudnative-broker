apiVersion: apps/v1
kind: Deployment
metadata:
  name: scores-app
  namespace: default
  labels:
    app: scores-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scores-app
  template:
    metadata:
      labels:
        app: scores-app
    spec:
      containers:
      - name: scores-app
        image: 992382531881.dkr.ecr.us-east-1.amazonaws.com/scores-app:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        env:
        - name: DATABASE_URI
          value: "postgresql://postgres:password@scores-db-service:5432/scores_db"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: scores-app-service
  namespace: default
spec:
  selector:
    app: scores-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
