apiVersion: apps/v1
kind: Deployment
metadata:
  name: offers-app
  namespace: default
  labels:
    app: offers-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: offers-app
  template:
    metadata:
      labels:
        app: offers-app
    spec:
      containers:
      - name: offers-app
        image: 992382531881.dkr.ecr.us-east-1.amazonaws.com/offers-app:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        env:
        - name: DATABASE_URI
          value: "postgresql://postgres:password@offers-db-service:5432/offers_db"
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: offers-app-service
  namespace: default
spec:
  selector:
    app: offers-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
