apiVersion: apps/v1
kind: Deployment
metadata:
  name: scores-db-deployment
  labels:
    app: scores-db
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scores-db
  template:
    metadata:
      labels:
        app: scores-db
    spec:
      containers:
      - name: scores-db
        image: postgres:13
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: "scores_db"
        - name: POSTGRES_USER
          value: "postgres"
        - name: POSTGRES_PASSWORD
          value: "password"
        volumeMounts:
        - name: scores-db-storage
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: scores-db-storage
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: scores-db-service
  namespace: default
spec:
  selector:
    app: scores-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP